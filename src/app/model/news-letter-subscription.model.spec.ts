import { NewsLetterSubscription } from './news-letter-subscription.model';

describe('NewsLetterSubscription', () => {
  it('should create an instance', () => {
    expect(new NewsLetterSubscription()).toBeTruthy();
  });
});
